/**
 * returns random number between one and ten.
 * @returns random number between one and ten.
 */
function rand10() {
    return (1 + Math.floor(Math.random() * 10));
}
console.log(rand10());

